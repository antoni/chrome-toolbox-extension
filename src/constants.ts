export const KEY_ENTER = 13, KEY_ESCAPE = 27, KEY_SPACE = 32,
             KEY_LEFT_ARROW = 37, KEY_RIGHT_ARROW = 39;

// Max time between 2 subsequent key presses to be qualified as
export const KEYPRESS_BETWEEN = 200;

// Battery check interval [ms], 2 mins
export const BATTERY_CHECK_INTERVAL = 120000;
export const THRESHOLD_DEFAULT = 0.20;
